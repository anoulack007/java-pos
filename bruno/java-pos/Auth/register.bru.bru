meta {
  name: register.bru
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "username": "{{adminUsername}}",
    "password": "{{adminPassword}}",
    "role": "ADMIN"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
