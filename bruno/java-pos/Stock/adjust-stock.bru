meta {
  name: adjust-stock
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/stocks/adjust
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "productId": "{{productId}}",
    "qtyChange": 100
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
