meta {
  name: get-stock
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/stocks/{{productId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
