meta {
  name: get-product
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/products/{{productId}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
