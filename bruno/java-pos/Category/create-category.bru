meta {
  name: create-category
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Snack"
  }
}

script:post-response {
    const data = res.body;
    if (data && data.data) {
  //     console.log(data)
      bru.setEnvVar("categoryId", data.data.id);
    }
}

settings {
  encodeUrl: true
  timeout: 0
}
